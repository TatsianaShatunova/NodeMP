#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../smartdc/bin/sdc-deletemachinesnapshot" "$@"
  ret=$?
else 
  node  "$basedir/../smartdc/bin/sdc-deletemachinesnapshot" "$@"
  ret=$?
fi
exit $ret
