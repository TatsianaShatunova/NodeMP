#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../smartdc/bin/sdc-getmachinemetadata" "$@"
  ret=$?
else 
  node  "$basedir/../smartdc/bin/sdc-getmachinemetadata" "$@"
  ret=$?
fi
exit $ret
