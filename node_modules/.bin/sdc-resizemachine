#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../smartdc/bin/sdc-resizemachine" "$@"
  ret=$?
else 
  node  "$basedir/../smartdc/bin/sdc-resizemachine" "$@"
  ret=$?
fi
exit $ret
